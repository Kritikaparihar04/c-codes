
vector<int> parseInts(string str) {
	// Complete this function
    vector<int>v;
    
    stringstream s(str);
    
  char ch;
   int num;
   while(s>>num)
   {
       v.push_back(num);
       s>>ch;
   }
     
    return v;
}
