#include<iostream>
using namespace std;
int main()
{
    int n=8 ,count=0;
   for(int i=2;i<n;i++)
   {
       if(n%i==0)
       {
           count++;
           break;
       }
   }
   if(count==0)
   cout<<"yes prime";
   else
   cout<<"not prime";
    
    return 0;
}
