#include<iostream>
using namespace std;
#include<vector>
int main()
{
   vector<int> v;
   for(int i=0;i<10;i++)
   {
     v.push_back(i);             //insert the element
   }
 
 cout<<"sixe is :"<<v.size()<<endl;
 v.pop_back();

 for(int i=0;i<v.size();i++)
 {
   cout<<v.at(i)<<" ";
 }

 cout<<"capacity is "<<v.capacity();

}
