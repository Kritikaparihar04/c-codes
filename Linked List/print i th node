void printIthNode(Node *head, int i)
{
    if(head==NULL) return;
int count =0;
Node *temp=head;
while(temp->next!=NULL && count<i)
{
    temp=temp->next;
    count++;
}
    if(count==i){
        cout<<temp->data;
    }
  
}
